name: Deploy academic site to GitHub Pages

on:
  push:
    branches: ["main"]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages-${{ github.event_name }}-${{ github.ref }}"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Setup Pages
        id: pages
        uses: actions/configure-pages@v5
      - name: Stage Scholar PDF in source tree
        run: |
          mkdir -p publications/2026-adm-ashtekar
          curl --fail --location --silent --show-error \
            https://raw.githubusercontent.com/ToelUl/adm-to-ashtekar-notes/main/main.pdf \
            --output publications/2026-adm-ashtekar/paper.pdf
          test -s publications/2026-adm-ashtekar/paper.pdf
          test $(stat -c%s publications/2026-adm-ashtekar/paper.pdf) -le 5242880
      - name: Build with Jekyll
        uses: actions/jekyll-build-pages@v1
        with:
          source: ./
          destination: ./_site
      - name: Verify staged Scholar PDF in built site
        run: |
          test -s _site/publications/2026-adm-ashtekar/paper.pdf
          test $(stat -c%s _site/publications/2026-adm-ashtekar/paper.pdf) -le 5242880
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v4

  deploy:
    if: github.event_name != 'pull_request'
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
